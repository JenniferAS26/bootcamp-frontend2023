/**
 * String: Podemos crear strings usando '' o ""
 * "Esto es un string" o 'Esto es un string'
 * 
 * Propiedades de los Strings
 * .length: Se usa para retornar la longitud de un string
 * "Hola!!".length //6
 * 
 * Metodos Basicos Stings
 * 'BLUE'.toLowerCase()
 * 
 * Visualizar variables (o expresiones)
 * console.log() se utiliza para visualizar una variable o una expresion
 * 
 * Character Access
 * Podemos acceder a un caracter especifico en un string usando la sintaxis de los corchetes rectagulares []
 * 
 * Operador suma (+)
 * Tiene diferentes comportamientos basado en los tipos valores que usamos 
 * 
 * Variables
 * Tenemos dos tipos de variables en JavaScript
 * let
 * const
 *  Recapitulacion de Variables
 * - Cuando usamos una variable por primera vez en JavaScript necesitas declararla con let y const
 * - Usamos const para variables que no necesitamos re-asignar
 * - Usamos let para variavles que necesitamos re-asignar despues (cambiar su valor en el futuro)
 * - Las variables creadas con const no pueden ser re-asignadas, entonces no puede tener el "=" seguido de la varible (su nombre) despues de ser declaradas.
 * - Si vemos var esto quiere decir que son codigos hechos con la version anterior de JavaScript. Podemos convertir estas variables en let y algunas veces en const si la variable no se reasigna
 * 
 */

const store = 'tienda'; // declaracion y asignacion

let test; // declaracion
test = 'prueba'; // asignacion

//console.log("Hola!!".length);
let text = 'Hello World!'; // declaracion y asignacion
//console.log(text.length);

//console.log('GREEN'.toLowerCase());
let textUpperCase = "nuevo texto en mayuscula";
//console.log(textUpperCase.toUpperCase());

let language = 'Ingles';
//console.log(language[0]); // I
//console.log(language[1]); // n
//console.log(language[2]); // g
//console.log(language[3]); // l
//console.log(language[4]); // e
//console.log(language[5]); // s

let firstWord = "Monica Rincon"
let secondWord = "Makaia"
// console.log(firstWord + " " + secondWord);
// console.log(`Este es el ${firstWord} de ${secondWord}`)
// console.log(firstWord)



